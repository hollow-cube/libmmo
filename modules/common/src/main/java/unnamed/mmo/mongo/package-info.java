/**
 * MongoDB utilities to work with the other systems present.
 * <p>
 * `common` does not have a runtime dependency on mongodb, so this package
 * will not bring along any extra baggage. However, any package using this
 * module must bring along mongodb-driver-sync.
 */
package unnamed.mmo.mongo;