package unnamed.mmo.loot;

public class TestLootPool {

    // Generate nothing if any condition fails (test first & second condition)

    // Should correctly choose based on weight (test using a known number generator)

    // Should apply modifiers to all generated items in order.


    // Codec tests
}
